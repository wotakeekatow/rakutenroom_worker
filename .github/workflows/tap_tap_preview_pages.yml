name: TAP TAP EVO RC6 Preview Branch Marker

on:
  workflow_dispatch:

jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Preview publishing is controlled by the default-branch verified publisher."
